import React from "react";
import ReactDOM from "react-dom/client";
import CaseFileScanner from "./CaseFileScanner.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CaseFileScanner />
  </React.StrictMode>
);